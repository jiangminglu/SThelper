<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="@android:style/Theme.Light">


    </style>

    <style name="actionTheme" parent="@android:style/Theme.Holo.Light">


        <item name="android:actionBarStyle">@style/ui_actionbar_style</item>
        <item name="android:actionMenuTextColor">#ffffff</item>
        <item name="android:titleTextStyle">@style/AcBar_titleStyle</item>
    </style>

    <style name="AcBar_titleStyle">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <style name="full_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:background">#ffffff</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- 自定义ActionBar样式，重设ActionBar背景，隐藏actionbar左侧的应用图标和标题 -->
    <style name="ui_actionbar_style" parent="@android:style/Widget.Holo.Light.ActionBar">

        <!-- 在样式里面直接替换掉actionbar的背景，避免在有的窗口进入慢时导致进入时显示系统默认的actionbar -->
        <item name="android:background">@color/app_default_actionbar_bg</item>
        <!-- 在样式里面去掉acionbar的应用图标和标题 -->
        <item name="android:displayOptions">useLogo</item>

    </style>


    <!-- 自定义ratingbar -->
    <style name="yellowRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/yellow_rating_bar</item>
        <item name="android:minHeight">38px</item>
        <item name="android:maxHeight">38px</item>
    </style>
</resources>
